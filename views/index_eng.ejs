<link rel="stylesheet" href="../css/style_min.css">
<script type="text/javascript" defer src="../js/script.js"></script>
<div class="flex flex-col">
    <div class="author">
        <h1 class="author_name"></h1>
        <p class="author_comment"></p>
    </div>
    <!-- <img src="path.jpg" alt="path through field to the clouds" class="author_photo m-0 m-auto pb-10" width="220"
        height="112"> -->
    <section class="mt-28 z-10 articles">
        <!--  <h2 class="articles_title font-bold text-2xl ml-5">Τελευταία Άρθρα</h2> -->
        <ul class="mt-2 mb-20 posts_ul">

        </ul>
    </section>
</div>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        let e = null, t = !1, n = !1, i = document.querySelector(".posts_ul"), a = null, l = !1; async function s(s = !1) {
            if (t || n) return; t = !0; let o = new URL("/more_posts", window.location.origin); e && o.searchParams.append("last_post", e), o.searchParams.append("limit", s ? 2 : 1), o.searchParams.append("cache_buster", (new Date).getTime()); try {
                let r = await fetch(o), d = await r.json(); if (d.length > 0) {
                    d.forEach(e => {
                        let t = document.createElement("li"); t.classList.add("wipe", "border-b-zinc-800", "border-b-4", "mb-11"), t.setAttribute("data-post-id", e._id), t.innerHTML = `
                        <a href="/post/${e._id}">
                            <span class=" ml-6 text-base hover:text-zinc-400 md:ml-24 xl:ml-32 2xl:ml-36 font-bold break-words" style="display:block; max-width:78vw;">
                                ${e.title}
                            </span>
                        </a>
                        <div class="ml-6 mb-4 cursor-default md:ml-24 xl:ml-32 2xl:ml-36">
                            <span class="font-light text-xs">
                                από ${e.username}
                            </span>
                        </div>
                        <div class="break-words flex my-0 mx-auto w-9/12">
                            <p class="mt-2 mb-6 font-light line-clamp-6 text-sm posts md:line-clamp-[7] xl:line-clamp-[10]">
                                ${e.body}
                            </p>
                        </div>
                        <div class="text-center mb-20">
                            <p class="mb-2 font-light text-xs cursor-default post_date">
                                -Posted: ${new Intl.DateTimeFormat("en-GB", { day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit", hour12: !1 }).format(new Date(e.date))}
                            </p>
                            <p class="font-light text-xs cursor-default post_date">
                                -Updated: ${new Intl.DateTimeFormat("en-GB", { day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit", hour12: !1 }).format(new Date(e.update_date))}
                            </p>
                        </div>
                    `, i.appendChild(t), a = t, requestAnimationFrame(() => { t.classList.add("visible") })
                    }), e = d[d.length - 1]._id; let m = document.createElement("div"); m.style.height = "50px", i.appendChild(m), a && !s && (l = !0, window.scrollTo({ top: a.offsetTop + a.offsetHeight - window.innerHeight, behavior: "smooth" })), d.length > 1 && (i.getElementsByTagName("li")[1].style.paddingTop = "2rem")
                } else n = !0
            } catch (c) { } finally { t = !1 }
        } window.scrollTo(0, 0), s(!0); let o = () => { l || function () { if (n) return void window.removeEventListener("scroll", o); window.innerHeight + window.scrollY >= document.body.scrollHeight - 100 && s() }(), l = !1 }, r; window.addEventListener("scroll", () => { r && clearTimeout(r), r = setTimeout(o, 10) })
    });</script>


<style>
    @keyframes wipeAnimation {
        from {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .wipe {
        opacity: 0;
        transform: translateY(20px);
    }

    .wipe.visible {
        animation: wipeAnimation 0.8s ease forwards;
    }
</style>