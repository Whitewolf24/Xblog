<link rel="stylesheet" href="../css/style_min.css">
<script type="text/javascript" defer src="../js/script.js"></script>
<div class="flex flex-col">
    <div class="author">
        <h1 class="author_name"></h1>
        <p class="author_comment"></p>
    </div>

    <section class="mt-28 z-10 articles">
        <ul class="mt-2 mb-20 posts_ul">
        </ul>
    </section>
</div>


<script>
    document.addEventListener("DOMContentLoaded", () => {
        let e = null, t = !1, n = !1, l = document.querySelector(".posts_ul"), i = null, a = !1; async function s(s = !1) {
            if (t || n) return; t = !0; let r = new URL("/more_posts", window.location.origin); e && r.searchParams.append("last_post", e), r.searchParams.append("limit", s ? 2 : 1), r.searchParams.append("cache_buster", (new Date).getTime()); try {
                let o = await fetch(r), d = await o.json(); if (d.length > 0) {
                    d.forEach(e => {
                        let t = document.createElement("li"); t.classList.add("wipe", "border-b-zinc-800", "border-b-4", "mb-11"), t.setAttribute("data-post-id", e._id), t.innerHTML = `
                        <a href="/post/${e._id}">
                            <span class=" ml-6 text-base hover:text-zinc-400 md:ml-24 xl:ml-32 2xl:ml-36 font-bold break-words" style="display:block; max-width:78vw;">
                                ${e.title}
                            </span>
                        </a>
                        <div class="ml-6 mb-4 cursor-default md:ml-24 xl:ml-32 2xl:ml-36">
                            <span class="font-light text-xs">
                                από ${e.username}
                            </span>
                        </div>
                        <div class="break-words flex my-0 mx-auto w-9/12">
                            <p class="mt-2 mb-6 font-light line-clamp-6 text-sm posts md:line-clamp-[7] xl:line-clamp-[10]">
                                ${e.body}
                            </p>
                        </div>
                        <div class="text-center mb-20">
                            <p class="mb-2 font-light text-xs cursor-default post_date">
                                -Ανέβηκε: ${new Intl.DateTimeFormat("el-GR", { day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit", hour12: !1 }).format(new Date(e.date))}
                            </p>
                            <p class="font-light text-xs cursor-default post_date">
                                -Ενημερώθηκε: ${new Intl.DateTimeFormat("el-GR", { day: "2-digit", month: "2-digit", year: "numeric", hour: "2-digit", minute: "2-digit", hour12: !1 }).format(new Date(e.update_date))}
                            </p>
                        </div>
                    `, l.appendChild(t), i = t, requestAnimationFrame(() => { t.classList.add("visible") })
                    }), e = d[d.length - 1]._id; let m = document.createElement("div"); m.style.height = "50px", l.appendChild(m), i && !s && (a = !0, window.scrollTo({ top: i.offsetTop + i.offsetHeight - window.innerHeight, behavior: "smooth" })), d.length > 1 && (l.getElementsByTagName("li")[1].style.paddingTop = "2rem")
                } else n = !0
            } catch (c) { } finally { t = !1 }
        } window.scrollTo(0, 0), s(!0); let r = () => { a || function () { if (n) return void window.removeEventListener("scroll", r); window.innerHeight + window.scrollY >= document.body.scrollHeight - 100 && s() }(), a = !1 }, o; window.addEventListener("scroll", () => { o && clearTimeout(o), o = setTimeout(r, 10) })
    });</script>


<style>
    @keyframes wipeAnimation {
        from {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .wipe {
        opacity: 0;
        transform: translateY(20px);
    }

    .wipe.visible {
        animation: wipeAnimation 0.8s ease forwards;
    }
</style>